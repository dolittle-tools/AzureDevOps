parameters:
  name: 'cascade_build'
  buildConfiguration: 'Release'
  pool: Default
  CASCADES: ''
  resources:
    repositories:
      - repository: root
        type: github
        name: $CASCADES
        endpoint: dolittle-tools

steps:
- template: azure-pipelines.yml@root
